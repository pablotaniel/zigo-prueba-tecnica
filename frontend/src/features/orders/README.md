# orders

Estructura objetivo:

```
features/orders/
├── api/orders.ts
├── components/
│   ├── OrderEntryScreen.tsx
│   ├── OrderDetailsCard.tsx
│   └── OrderLineForm.tsx
├── types/order.ts
└── hooks/useOrder.ts
```

Estado actual: código en `src/types/`, `src/api/`, `src/components/`. Pantalla de ingreso/detalle iniciada. Listado y alta completa pendientes.
